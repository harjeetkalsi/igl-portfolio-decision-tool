[project]
name = "igl-portfolio-tool"
version = "0.1.0"
description = "Prototype decision support tool for comparing research funding portfolios."
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
    "openai>=1.0.0",
    "gunicorn>=22.0.0",
]

